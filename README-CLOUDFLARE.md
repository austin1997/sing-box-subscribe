# sing-box-subscribe: Cloudflare Workers port

Add the files in this directory to the root of `Toperlock/sing-box-subscribe` (or a fork), then follow `docs/cloudflare-workers.md`.

The implementation is deliberately additive: upstream Vercel/Docker/CLI behavior stays intact, while `worker.py` is the Cloudflare entrypoint.
